<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AlexonTask" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_color</item>
        <item name="colorPrimaryVariant">@color/main_color</item>
        <item name="colorOnPrimary">@color/accent_color</item>
        <item name="colorAccent">@color/accent_color</item>

        <item name="android:navigationBarColor">@color/main_color</item>
        <item name="android:windowBackground">@color/main_color</item>
        <item name="android:textViewStyle">@style/TextViewStyle</item>
        <item name="android:fontFamily">@font/font_family</item>

        <!--Changing the statusBar color to black to support API 22 and below-->
        <item name="android:statusBarColor">@color/black</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.App.Login" parent="Theme.AlexonTask">
        <item name="android:statusBarColor">@color/accent_color</item>
        <item name="android:navigationBarColor">@color/main_color</item>

    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background_color</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_inset</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.AlexonTask</item>
    </style>

    <style name="TextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/font_family</item>
    </style>

    <style name="AppButton" parent="Widget.AppCompat.ActionButton">
        <item name="android:background">@drawable/app_button_background</item>
        <item name="android:elevation">10dp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:padding">20dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">17sp</item>
    </style>


</resources>